{
  "name": "wallpaper-app",
  "version": "3.4.1",
  "description": "A Windows background application that syncs wallpapers and manages a local collection.",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "pack": "npx electron-builder --dir",
    "dist": "npx electron-builder",
    "publish": "npx electron-builder --win --publish always"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/Debanjan110d/wallpaper-sync-app.git"
  },
  "build": {
    "appId": "com.wallpaper.syncapp",
    "productName": "Wallpaper Sync App",
    "win": {
      "target": "nsis",
      "icon": "icon.png",
      "cscLink": "scripts/self-signed-cert.pfx",
      "cscKeyPassword": "password"
    },
    "asar": true,
    "extraResources": [
      {
        "from": "wall_img",
        "to": "wall_img",
        "filter": [
          "**/*"
        ]
      }
    ],
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true
    },
    "publish": [
      {
        "provider": "github",
        "releaseType": "release"
      }
    ]
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "latest",
    "electron-updater": "^6.6.2",
    "wallpaper": "latest"
  },
  "devDependencies": {
    "electron": "latest",
    "electron-builder": "^26.8.1"
  }
}
